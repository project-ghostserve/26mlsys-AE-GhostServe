# Core runtime
requests>=2.31.0
openai>=1.30.0

# Model/config loading
transformers>=4.40.0
huggingface-hub>=0.23.0
sentencepiece>=0.1.99
safetensors>=0.4.3

# PyTorch (CUDA build should be installed to match your system)
# Recommended: install torch separately from https://pytorch.org/get-started/locally/
torch>=2.3.0

# Optional but commonly needed by transformers/tokenizers stacks
numpy>=1.24.0
packaging>=23.0
